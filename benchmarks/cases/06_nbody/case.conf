args = 50000
cxx_flags = -ffp-contract=off
description = A five-body gravity simulation with a square root written by hand. C++ uses -ffp-contract=off because Flow-Wing emits no FMA instructions. Without that flag the two results differ after about 5000 steps.

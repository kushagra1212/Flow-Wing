class A {
  var x:int 
  init(x:int) -> nthg {
      self.x = x 
    }
}

var a:A = Nir

if(a == Nir) {
  print("A")
}
/; fun sum(n: int) -> int {
  /;   var sum: int = 0
  /;   for (var i = 0 to n : 1) {
  /;     sum = sum + i 
  /;   }
  /;   return sum
  /; }

/; print(sum(10))

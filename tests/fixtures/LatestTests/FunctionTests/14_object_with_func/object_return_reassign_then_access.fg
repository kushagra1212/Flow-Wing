type Person = {
  name: str
  age: int
}

fun makePerson(n: str, a: int) -> Person {
  var p: Person = {name: n, age: a}
  return p
}

var p: Person = makePerson("Alice", 30)
println(p.name)
p = makePerson("Bob", 40)
println(p.name)
println(p.age)

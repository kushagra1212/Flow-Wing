type Person = {
  name: str
  age: int
}

fun makePerson(n: str, a: int) -> Person {
  var p: Person = {name: n, age: a}
  return p
}

var p: Person = makePerson("Bob", 25)
println(p.name)
println(p.age)

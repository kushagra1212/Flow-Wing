class A {
  var x: int
  fun init(x: int) -> nthg {
    self.x = x
  }
}

fun createA(val: int) -> A {
  return new A(val)
}

fun doubleA(a: A) -> A {
  return new A(a.x * 2)
}

var a: A = createA(10)
var b: A = doubleA(a)
println(a.x)
println(b.x)

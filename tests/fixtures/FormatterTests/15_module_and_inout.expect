/; from LatestTests/ModuleTests/01 + FunctionTests/05_inout (messy, single file)
module[m]
var secret: int = 40
fun bump(inout x: int) -> nthg {
    x = x + 1
}
fun main() -> nthg {
    println(m::secret)
    var t: int = 1
    m::bump(t)
    println(t)
}

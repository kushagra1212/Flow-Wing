type Position = {
  x: int,
  y: int
}

const screenWidth: int  = 1000
const screenHeight: int = 500
const side: int = 10

fun getN() -> int {
  return 2
}
  var pos: Position = { 
    x: getN(), 
    y: getN()
  }
  print(pos.x)
  pos.x =  pos.x -  pos.x % side
  pos.y = pos.y - pos.y % side
